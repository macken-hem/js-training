%h1 My Images
%div
= form_for(@image, url:'/mypage', metho: :create,class: "dropzone") do |f|
  .fallback
  =  f.file_field :file_name, multiple: true
-# <%= form_for(Image.new, html: {multipart: true, class: "dropzone"}) do |f|  %>
-#   <div class="fallback">
-#     <%= f.file_field :file_name, multiple: true %>
-#   </div>
-# <% end %>

